import { AdminProductPackHub } from "@/features/staff";

export const metadata = {
	title: "Product & Pack Hub — PixelMage Admin",
	description: "Quản lý sản phẩm và pack đồng bộ",
};

export default function ProductsPage() {
	return <AdminProductPackHub />;
}
