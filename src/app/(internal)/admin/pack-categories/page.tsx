import { AdminPackCategoriesPage } from "@/features/staff";

export const metadata = {
	title: "Pack Categories — PixelMage Admin",
	description: "Quản lý Pack Category — cấu hình tỷ lệ Gacha & Card Pool",
};

export default function PackCategoriesPage() {
	return <AdminPackCategoriesPage />;
}
